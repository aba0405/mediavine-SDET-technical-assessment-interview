{
  "name": "cypress-quick-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "cypress run --headed",
    "cy:run:headless": "cypress run",
    "start": "npm run cy:run --spec 'cypress/e2e/**/*' -b chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.7.0",
    "cypress-downloadfile": "^1.2.3"
  }
}